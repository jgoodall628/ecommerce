<h1>All <%= @category.name %></h1>

<div class="row">
  <% @products.each do |product| %>
    <div class="col-md-4">
      <%= render partial: "storefront/product_tile", locals: {product: product} %>

    </div>
    <% end %>
</div>
